{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "scripts": {
    "dev": "balm --config config/balm.web.js",
    "build:web": "balm -p --config config/balm.web.js",
    "build:app": "balm -p -electron --config config/balm.app.js",
    "prod": "npm run build:web && npm run build:app"
  },
  "devDependencies": {
    "asar": "^3.0.0",
    "balm": "^3.1.0",
    "electron": "^10.1.0",
    "electron-packager": "^15.1.0"
  },
  "private": true
}
