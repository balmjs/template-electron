{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "scripts": {
    "dev": "gulp --silent",
    "start": "./node_modules/.bin/electron ./app --debug",
    "prod": "gulp --silent -p"
  },
  "devDependencies": {
    "asar": "^2.0.1",
    "balm": "^2.0.4",
    "electron": "^7.1.2",
    "electron-packager": "^14.1.1",
    "gulp": "^4.0.2"
  },
  "private": true
}
