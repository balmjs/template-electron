{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "scripts": {
    "dev": "gulp --silent",
    "start": "./node_modules/.bin/electron ./app --debug",
    "prod": "gulp --silent -p"
  },
  "devDependencies": {
    "asar": "^2.1.0",
    "balm": "^2.8.0",
    "electron": "^6.1.8",
    "electron-packager": "^14.2.1",
    "gulp": "^4.0.2"
  },
  "private": true
}
