{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "scripts": {
    "dev": "gulp --silent",
    "start": "./node_modules/.bin/electron ./app --debug",
    "prod": "gulp --silent -p"
  },
  "devDependencies": {
    "asar": "^2.0.3",
    "balm": "^2.3.0",
    "electron": "^6.1.7",
    "electron-packager": "^14.2.0",
    "gulp": "^4.0.2"
  },
  "private": true
}
